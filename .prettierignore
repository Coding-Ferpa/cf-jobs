# A especificação de arquitetura é canônica: não reformatar (evita ruído em diffs).
docs/

.next/
coverage/
node_modules/
out/
playwright-report/
test-results/
pnpm-lock.yaml

# Bundle da UI Scalar, copiado no build por scripts/copy-scalar.ts.
public/scalar/

# Fixtures reproduzem respostas reais, inclusive as malformadas: formatá-las
# conserta justamente o defeito que o teste precisa reproduzir.
src/features/import/__fixtures__/
