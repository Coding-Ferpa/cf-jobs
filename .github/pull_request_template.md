# O que muda

<!-- Uma ou duas frases. Se fecha uma issue, escreva "Closes #123". -->

## Por quê

<!-- O problema que isso resolve. O "como" já está no diff; o "porquê" não. -->

## Como testar

<!-- Passos para quem revisa reproduzir o efeito da mudança. -->

1.
2.

## Checklist

- [ ] `pnpm lint`, `pnpm typecheck` e `pnpm test` passam localmente
- [ ] Testes cobrindo a mudança (obrigatório para lógica em `src/features` e `src/lib`)
- [ ] Documentação atualizada quando necessário (README, `.env.example`, `docs/`)
- [ ] Sem segredos, chaves ou dados pessoais no diff
- [ ] Texto de interface em pt-BR
- [ ] Mudança de arquitetura registrada em `docs/adr/` (se aplicável)

## Screenshots (mudanças de UI)

<!-- Mobile (375px) e desktop (1280px). Remova esta seção se não houver UI. -->
