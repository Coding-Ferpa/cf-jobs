# Copie este arquivo para `.env` e preencha os valores.
# Nunca commite `.env`. As chaves aqui precisam corresponder ao schema de
# src/lib/env.ts — `pnpm check:env` falha o CI se divergirem.

# --- Público (vai para o bundle do navegador: sem segredos) ---

# URL canônica do site. Em desenvolvimento: http://localhost:3000
NEXT_PUBLIC_SITE_URL=
# URL do projeto Supabase. Local (`supabase start`): http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_URL=
# Chave anônima do Supabase — a RLS é quem protege os dados
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# DSN do Sentry (doc 09). Opcional: em branco, o SDK não é carregado nem no
# servidor nem no navegador, e o projeto funciona igual.
NEXT_PUBLIC_SENTRY_DSN=

# --- Servidor (segredos: apenas na Vercel/Supabase, nunca no repositório) ---

# Usada exclusivamente em Server Actions que já validaram sessão e papel
SUPABASE_SERVICE_ROLE_KEY=
# Pooler Supavisor (transaction mode) usado pelo Drizzle em runtime
DATABASE_URL=
# Conexão direta, usada por migrations
DIRECT_URL=
# Chave da NVIDIA NIM (build.nvidia.com). Opcional: só a importação por IA
# precisa dela — o resto do projeto, inclusive o login, sobe sem.
NVIDIA_API_KEY=
NVIDIA_API_KEY_FALLBACK=
# Endpoint compatível com a API OpenAI. Em branco usa o NIM da NVIDIA; um
# deploy da comunidade pode apontar para outro provedor, e o E2E aponta para
# o dublê local.
AI_BASE_URL=
# Modelos NIM — deixe em branco para usar os padrões do código
AI_MODEL_PRIMARY=
AI_MODEL_SECONDARY=
AI_MODEL_FALLBACK=
# Teto mensal de tokens. Opcional: sem ela o painel de consumo continua,
# só não há bloqueio suave (doc 05).
AI_MONTHLY_TOKEN_BUDGET=
# Segredo dos endpoints acionados por cron (mínimo de 16 caracteres)
CRON_SECRET=

# Sal do hash anônimo de visitante (mínimo de 16 caracteres). Sem ele o
# endpoint de eventos responde 503 — o resto do app funciona normalmente.
ANALYTICS_SALT=

# --- GitHub OAuth (opcional) ---
# Sem estas variáveis o botão "Entrar com GitHub" não é exibido e o login
# acontece só por e-mail e senha. Preencha ao criar o OAuth App do GitHub;
# a URL de callback é {NEXT_PUBLIC_SUPABASE_URL}/auth/v1/callback.
SUPABASE_AUTH_EXTERNAL_GITHUB_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GITHUB_SECRET=
